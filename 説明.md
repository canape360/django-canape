# Django 開発・管理画面表示の手順（整理版）

2026/1/23 Render →本番成功[日記アプリ](https://django-canape-s664.onrender.com/)
                         [Django 管理サイト](https://django-canape-s664.onrender.com/admin/)

参考：[itc.tokyo Django チュートリアル](https://itc.tokyo/django/tutorial/)

---

## 1. プロジェクトディレクトリに移動

```bash
cd ~/projects/django-canape
cd /Users/user1/projects/django-canape  :前に同じ

仮想環境の起動
source myenv/bin/activate

src ディレクトリへ移動
cd src

開発用サーバーの起動
python3 manage.py runserver  ;ポートを変更する場合は       など。python3 manage.py runserver 8001



一般ユーザーアクセス👇

http://127.0.0.1:8001/accounts/signup/

http://127.0.0.1:8001/accounts/login/


http://127.0.0.1:8001/myapp/dashboard/    :一般ユーザー専用ページ
```

## 2. 管理画面でのデータ登録・編集

```bash
python3 manage.py shell     ;Python シェルでデータ確認・変更


# MyApp テーブルからid と title だけを取り出して一覧で返す
MyApp.objects.values_list('id', 'title')  

# 現在の名前空間 に存在するもの一覧
dir()

- **MyApp**  
  日記モデル（DBテーブル）

- **MyMail**  
  別のモデル

- **Person**  
  人物モデル

- **models**  
  Django の `models` モジュール

- **settings**  
  Django の設定

- **__builtins__**  
  Python の組み込み機能（無視でOK）


from myapp.models import MyApp  :この後以下を実行する
# データ取得
q = MyModel.objects.get(id=3)
# データ表示
q.title
q.content

# データ変更
q.title = "新しいタイトル"
q.save()


```

## ユーザー登録と確認を「シェルだけ」で体験

### Userモデルを読み込む(shell を起動のあと)

```bash
from django.contrib.auth.models import User
```

### ユーザー登録

```bash
user = User.objects.create_user(
    username="testuser",
    email="test@example.com",
    password="password123"
)
```

### 登録されたか確認

```bash
User.objects.all()

user.username
user.email
```

### ログインできるか確認（認証）

```bash
from django.contrib.auth import authenticate

user = authenticate(username="testuser", password="password123")
user

#結果 成功 → <User: testuser>  失敗 → None
```

